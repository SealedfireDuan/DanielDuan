#ifndef _REGEX_WORD_H_
#define _REGEX_WORD_H_

int GetChoice(char* chr);

int GetWord(char* wd);

int GetRegex(char* str);

int StoreWord(char* wd, char* fil);

int SearchForWord(char* input, char* fil);

#endif